Joe.Map.EntityFramework
=======================

IDbViewContext implementation for EntityFramework

To Use bidirectional mapping with Joe.Map simply inherit from `Joe.MapBack.MapBackDbContext` instead of DbContext
